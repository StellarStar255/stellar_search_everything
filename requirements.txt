# Qt 界面框架（FileSearchQt.py，当前主版本）
PySide6>=6.7

# 图标生成/转换（assets/make_icon.py、CI 打包时 icns→ico）
Pillow>=12.0.0

# --- 以下仅旧版 Tkinter 界面（FileSearchGUI.py）需要 ---
# macOS 剪贴板文件复制功能（AppKit，仅 macOS 需要）
pyobjc-framework-Cocoa>=12.1; sys_platform == "darwin"
